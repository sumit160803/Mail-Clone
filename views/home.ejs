<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <title>Homepage</title>
    <style>
        body {
            background-color: #000; /* Black background */
            color: #fff; /* White text color */
            display: flex; /* Centering the form */
            justify-content: center; /* Center horizontally */
            align-items: center; /* Center vertically */
            height: 100vh; /* Full viewport height */
            margin: 0; /* Remove default margin */
            font-family: 'Poppins', sans-serif;
        }

        h1 {
            text-align: center; /* Center the heading */
            margin-bottom: 30px; /* Spacing below heading */
            font-weight: 600;
        }

        .form-container {
            background-color: #1c1c1c; /* Dark grey background for form */
            padding: 30px; /* Padding inside the form container */
            border-radius: 10px; /* Rounded corners */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5); /* Shadow effect for highlighting */
            max-width: 400px; /* Maximum width for form container */
            width: 100%; /* Full width for small screens */
        }

        input,
        textarea {
            background-color: #333; /* Darker background for input fields */
            color: #fff; /* White text for input fields */
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded corners for input fields */
            font-family: 'Poppins', sans-serif;
        }

        input::placeholder,
        textarea::placeholder {
            color: #bbb; /* Lighter color for placeholder text */
        }

        button {
            width: 100%; /* Full width button */
            background-color: #007bff; /* Blue button background */
            color: #fff; /* White button text */
            border: none; /* Remove default border */
            padding: 10px; /* Padding inside button */
            border-radius: 5px; /* Rounded corners for button */
            margin-top: 15px; /* Space above the button */
            font-family: 'Poppins', sans-serif; /* Apply Poppins font */
            font-weight: 600; 
        }

        button:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h1>Email sender and receiver</h1>
        <form method="POST" action="/send-mail">
            <label for="sender">Sender's Mail:</label><br>
            <input type="email" name="sender" id="sender" placeholder="Enter your email" class="form-control mb-3">
            <label for="to">Receiver's Mail:</label><br>
            <input type="email" name="to" id="to" placeholder="Enter receiver's mail" class="form-control mb-3">
            <label for="subject">Subject:</label><br>
            <input type="text" name="subject" id="subject" placeholder="Enter Subject" class="form-control mb-3">
            <label for="body">Body:</label><br>
            <textarea name="body" id="body" placeholder="Enter the content" class="form-control mb-3"></textarea>
            <button type="submit" class="btn btn-primary">Enter to Submit</button>
        </form>
    </div>
</body>

</html>
